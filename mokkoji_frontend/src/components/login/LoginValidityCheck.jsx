import Login from "./Login";
import userInfo from "./UserInforData";

export default function LoginValidityCheck({ inputId, inputPw }) {
    const inputUserId = { inputId }
    const inputUserPW = { inputPw }
    return (
        <div>

        </div>
    );
}

